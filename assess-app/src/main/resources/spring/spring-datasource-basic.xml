<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
	    http://www.springframework.org/schema/tx
	    http://www.springframework.org/schema/tx/spring-tx.xsd">

    <description>数据源配置</description>

    <bean id="masterDataSourceBasic" class="org.apache.tomcat.jdbc.pool.DataSource" destroy-method="close">
        <property name="poolProperties">
            <bean class="org.apache.tomcat.jdbc.pool.PoolProperties">
                <property name="driverClassName" value="${database.driver}"/>
                <property name="url" value="${master.database.url.basic}"/>
                <property name="username" value="${database.username}"/>
                <property name="password" value="${database.password}"/>
                <property name="jmxEnabled" value="true"/>
                <property name="testWhileIdle" value="true"/>
                <property name="testOnBorrow" value="false"/>
                <property name="testOnReturn" value="false"/>
                <property name="validationInterval" value="30000"/>
                <property name="validationQuery" value="SELECT 1"/>
                <property name="timeBetweenEvictionRunsMillis" value="30000"/>
                <property name="maxWait" value="60000"/>
                <property name="minEvictableIdleTimeMillis" value="30000"/>
                <property name="maxActive" value="50"/>
                <property name="initialSize" value="10"/>
                <property name="maxIdle" value="10"/>
                <property name="minIdle" value="5"/>
                <property name="removeAbandoned" value="true"/>
                <property name="removeAbandonedTimeout" value="180"/>
                <property name="logAbandoned" value="true"/>
            </bean>
        </property>
    </bean>

    <!-- ======================================= -->
    <!--事务管理DataSourceTransactionManager -->
    <!-- ======================================= -->
    <bean id="transactionManagerBasic"
          class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="masterDataSourceBasic"/>
        <property name="nestedTransactionAllowed" value="true"/>
    </bean>

    <!-- ======================================= -->
    <!-- sqlSessionFactory -->
    <!-- ======================================= -->
    <bean id="sqlSessionFactoryBasic" class="org.mybatis.spring.SqlSessionFactoryBean">
        <property name="configLocation">
            <value>classpath:mybatis-config.xml</value>
        </property>
        <property name="dataSource" ref="masterDataSourceBasic"/>
        <property name="mapperLocations" value="classpath:mybatis/basic/**/*.xml"/>
    </bean>

    <!-- =========================================== -->
    <!-- 通过@Mapper注解的形式使用Mapper Interface -->
    <!-- =========================================== -->
    <bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
        <property name="basePackage" value="com.copower.pmcc.assess.dal.basic"/>
        <property name="sqlSessionFactoryBeanName" value="sqlSessionFactoryBasic"/>
    </bean>



</beans>